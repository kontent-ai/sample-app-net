@{
    @model DancingGoat.Models.MoreButtonPartialViewModel
}
<div class="row">
    <div class="clear center-text">
        <localized-route asp-controller="@Model.Controller" asp-action="@Model.Action" class="btn btn-more">
            @Model.Text
        </localized-route>
    </div>
</div>
